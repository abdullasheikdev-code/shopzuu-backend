# Server
server.port=8080
spring.application.name=shopzuu

# PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/shopzuu
spring.datasource.username=postgres
spring.datasource.password=Shopflow123
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT
jwt.secret=shopzuu_super_secret_key_minimum_256_bits_long_string_here_change_this
jwt.expiration=86400000

# Stripe
stripe.secret.key=sk_test_your_stripe_secret_key
stripe.webhook.secret=whsec_your_webhook_secret

# Razorpay
razorpay.key.id=your_razorpay_key_id
razorpay.key.secret=your_razorpay_key_secret

# Cloudinary
cloudinary.cloud-name=your_cloud_name
cloudinary.api-key=your_api_key
cloudinary.api-secret=your_api_secret

# File Upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

spring.profiles.active=prod